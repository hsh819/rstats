# 核心
pandas>=2.0
openpyxl>=3.1

# PDF 解析（任务一 财报抽取 + 任务三 研报切块）
pdfplumber>=0.11
pymupdf>=1.24            # 渲染 PDF 页到 JPG（paper_image）

# 可视化
matplotlib>=3.7

# RAG / 文本检索
scikit-learn>=1.3        # TfidfVectorizer + cosine_similarity

# LLM（OpenAI 兼容；可选，无 KEY 自动降级到规则路径）
openai>=1.40

# HTTP + 进度
requests>=2.31
tqdm>=4.66

# 重试（llm_client 调用失败重试）
tenacity>=8.2
